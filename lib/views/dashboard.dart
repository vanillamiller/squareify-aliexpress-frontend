import 'package:flutter/cupertino.dart';

class Dashboard extends StatelessWidget {
  static const path = '/dashboard';
  @override
  Widget build(BuildContext context) => Center(child: Text('dash'));
}
